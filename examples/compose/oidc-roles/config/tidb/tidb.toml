# TiDB configuration for OIDC role-based scenario.
# initialize-sql-file runs on first data-dir initialization.

initialize-sql-file = "/etc/tidb/init.sql"

[security]
ssl-ca = "/pki/ca/root_ca.crt"
ssl-cert = "/pki/tidb/server.crt"
ssl-key = "/pki/tidb/server.key"
