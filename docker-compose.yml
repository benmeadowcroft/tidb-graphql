# Root convenience compose file.
# This keeps the two-command onboarding flow while delegating to the
# canonical quickstart scenario configuration.
include:
  - ./examples/compose/quickstart/docker-compose.yml
